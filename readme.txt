1. npm install babel-cli babel-preset-es2015 babel-preset-stage-2 --save-dev  
--babel-preset-es2015 : allows us to use 2015 
-- babel-preset-stage-2 : allows us to use next version of javascript
--babel/cli is a tool that allows you to use babel from the terminal


//map, filter, reduce

2. install express